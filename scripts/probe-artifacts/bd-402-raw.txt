# Raw BD 402 response captured 2026-05-31 01:37 UTC against:
#   POST https://netapps.ocfl.net/BestJail/Home/getInmates/SMITH via BD residential

HTTP/1.1 402 Residential Failed (bad_endpoint)
x-brd-err-code: policy_20130
x-brd-err-msg: Residential Failed (bad_endpoint): Requested site is not available for immediate residential (no KYC) access mode due to the fact that POST requests are not allowed. To get full residential access for targeting this site, fill in the KYC form: https://brightdata.com/cp/kyc
x-brd-error: (same as above)
x-brd-error-code: bad_endpoint

# Diagnosis: NOT billing. NOT zone-disabled. NOT quota-exhausted.
# Root cause: BD residential network requires KYC verification before allowing
# POST requests to target sites that aren't on BD's pre-approved no-KYC allowlist.
# OCFL's API requires POST for getInmates / getInmateDetails / getCharges.
#
# Fix options (in priority order):
#   1. Submit KYC at https://brightdata.com/cp/kyc — unlocks POST on residential
#      for the flipops_scraper zone. Required for legal cover anyway.
#   2. Switch the flipops_scraper zone product type to one that allows POST without
#      KYC: Datacenter (cheap but DC IPs are geo-fingerprinted), Web Unlocker
#      (BD-managed end-to-end), or ISP residential.
#   3. Add a per-scraper fallback that GETs a search-results page instead of
#      POSTing when the target permits — costly, target-specific rewrite.
